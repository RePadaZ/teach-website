{
  "name": "teach-website",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "b": "pnpm --filter @ideanick/backend",
    "f": "pnpm --filter @ideanick/frontend",
    "parDev": "pnpm -r -parallel dev",
    "parTypes": "pnpm -r -parallel types"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}